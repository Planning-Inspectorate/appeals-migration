# Horizon

This folder contains code relating to the source system, Horizon.

The `api` folder includes code for interacting with the JSON API, which is unlikely to be used.

The `web` folder includes code for interacting with the web functions, which will be used for migrating documents.